{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && corepack enable && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && node server.cjs"
  }
}
